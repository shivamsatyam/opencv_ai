import cv2 as cv
import numpy as np

img = cv.imread("photos/cat.jpg")

def translate(img,x,y):
	dimension = (img.shape[1],img.shape[0])
	transMatrix = np.float

cv.imshow('flip',img)

cv.waitKey(0)

